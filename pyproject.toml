[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-agent-system"
version = "1.0.0"
description = "AI Agent System using Scout-Maker-Checker-Curator pattern"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [
    {name = "AI Agent System", email = "info@example.com"}
]
keywords = ["ai", "agents", "document-processing", "workflow", "automation"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "pydantic>=2.0.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
    "mypy>=1.4.0",
    "flake8>=6.0.0",
    "black>=23.0.0",
]
pdf = [
    "PyPDF2>=3.0.0",
    "pdfplumber>=0.9.0",
]
docx = [
    "python-docx>=0.8.11",
]
nlp = [
    "langdetect>=1.0.9",
    "spacy>=3.5.0",
]
all = [
    "ai-agent-system[dev,pdf,docx,nlp]",
]

[project.urls]
Homepage = "https://github.com/example/ai-agent-system"
Documentation = "https://github.com/example/ai-agent-system/docs"
Repository = "https://github.com/example/ai-agent-system"
Issues = "https://github.com/example/ai-agent-system/issues"

[tool.setuptools]
packages = ["agents", "models", "parsers", "workflows"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false
disallow_incomplete_defs = false
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true

[tool.black]
line-length = 100
target-version = ['py39', 'py310', 'py311']
include = '\.pyi?$'
extend-exclude = '''
/(
  # directories
  \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | build
  | dist
)/
'''

[tool.flake8]
max-line-length = 100
extend-ignore = ["E203", "W503"]
exclude = [
    ".git",
    "__pycache__",
    "build",
    "dist",
    ".venv",
]
